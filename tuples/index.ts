// const names = [];
// names.push('Marcello', 123, true);
        //  String, number, boolean

        // const strinNumber: [string, number] = ['Marcello', 40];

            // Seu erro pq estou no TS
    //   const [name, setName]   = useState("Marcello")

//     let rgb: [number, number, number] = [255, 0, 0]

let bgColor: [number, number, number, number?];
bgColor = [0, 255, 255]
